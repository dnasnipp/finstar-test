import { createStore } from 'vuex'
import users from "@/store/users";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    users
  }
})
